import React from 'react';
import {View} from 'react-native';
import MobileContainer from './src/navigation/mobileContainer';

const App = () => {
  return (
    <View style={{flex: 1}}>
      <MobileContainer />
    </View>
  );
};

export default App;
